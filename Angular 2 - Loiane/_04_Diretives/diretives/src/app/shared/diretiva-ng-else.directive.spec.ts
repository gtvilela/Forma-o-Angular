import { DiretivaNgElseDirective } from './diretiva-ng-else.directive';

describe('DiretivaNgElseDirective', () => {
  it('should create an instance', () => {
    const directive = new DiretivaNgElseDirective();
    expect(directive).toBeTruthy();
  });
});
